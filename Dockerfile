# VintageStory Server Manager
# Multi-stage build defined in future stories

# Placeholder - will be replaced with actual build stages
FROM alpine:latest AS placeholder
RUN echo "Dockerfile placeholder - build stages defined in future stories"
